// MUI
import { styled as styledMUI } from "@mui/material/styles";
import { TextField } from "@mui/material";

// Styled
//import styled from "styled-components";

export const Search = styledMUI(TextField)`
    .MuiInputBase-root {
        background-color: #FFF;
    }
`;
