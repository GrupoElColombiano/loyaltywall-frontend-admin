import { FormControl, TextField } from "@mui/material";
import { styled } from "@mui/material/styles";

export const SearchProduct = styled(TextField)({
});

export const SearchSite = styled(FormControl)({
    color: "#000000DE"
});

export const SearchCategory = styled(FormControl)({
});
